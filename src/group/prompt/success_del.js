const ko = (name) => `🗑️ 그룹 ${name}이(가) 성공적으로 삭제되었습니다.`;

const en = (name) => `🗑️ Group ${name} deleted successfully.`;

const successDeletePrompt = {
  ko,
  en,
};

export default successDeletePrompt;
